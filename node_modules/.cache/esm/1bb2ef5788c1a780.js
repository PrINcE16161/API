let JWT_SECRET,jwt;_88c‍.x([["default",()=>_88c‍.o]]);_88c‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_88c‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
    static sign(payload, expiry = '1y', secret = JWT_SECRET) {
        return jwt.sign(payload, secret, { expiresIn: expiry });
    }

    static verify(token, secret = JWT_SECRET) {
        return jwt.verify(token, secret);
    }
}

_88c‍.d(JwtService);